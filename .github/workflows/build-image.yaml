name: Build and Publish Container

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
    branches: [main]
  # Rebuild daily to pick up new freshell releases and security patches
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  resolve-freshell:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.value }}
    steps:
      - name: Determine Freshell versions
        id: matrix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Latest stable (non-prerelease) release
          STABLE=$(curl -sL -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/danshapiro/freshell/releases/latest \
            | jq -r '.tag_name')

          if [ -z "$STABLE" ] || [ "$STABLE" = "null" ]; then
            echo "ERROR: Could not determine stable Freshell version"
            exit 1
          fi

          # Latest prerelease (release candidate)
          RC=$(curl -sL -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/danshapiro/freshell/releases \
            | jq -r '[.[] | select(.prerelease)] | sort_by(.created_at) | last | .tag_name // empty')

          # Compare versions: if RC is newer than stable, build it separately.
          # Otherwise (no RC, or RC is older/equal), rc tag points at stable.
          RC_IS_NEWER=false
          if [ -n "$RC" ] && [ "$RC" != "$STABLE" ]; then
            STABLE_SORT=$(echo "$STABLE" | sed 's/^v//')
            RC_SORT=$(echo "$RC" | sed 's/^v//')
            NEWER=$(printf '%s\n%s\n' "$STABLE_SORT" "$RC_SORT" \
              | sort -t. -k1,1n -k2,2n -k3,3n | tail -n1)
            if [ "$NEWER" = "$RC_SORT" ]; then
              RC_IS_NEWER=true
            fi
          fi

          # One matrix entry per channel — each job builds both full and lite targets
          if [ "$RC_IS_NEWER" = "true" ]; then
            MATRIX=$(jq -nc \
              --arg sv "$STABLE" --arg rv "$RC" \
              '{include:[
                {channel:"stable", freshell_version:$sv},
                {channel:"rc",     freshell_version:$rv}
              ]}')
          else
            MATRIX=$(jq -nc \
              --arg sv "$STABLE" \
              '{include:[
                {channel:"stable-and-rc", freshell_version:$sv}
              ]}')
          fi

          echo "Stable: $STABLE, RC: ${RC:-none}, RC is newer: $RC_IS_NEWER"
          echo "value=$MATRIX" >> "$GITHUB_OUTPUT"

  build-and-push:
    needs: resolve-freshell
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix: ${{ fromJson(needs.resolve-freshell.outputs.matrix) }}
      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # --- Full variant ---

      - name: Extract metadata (full)
        id: meta-full
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable=${{ matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=raw,value=rc,enable=${{ matrix.channel != 'stable' && github.ref == 'refs/heads/main' }}
            type=raw,value=${{ matrix.freshell_version }},enable=${{ matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=sha,enable=${{ matrix.channel != 'rc' }}
            type=sha,suffix=-rc,enable=${{ matrix.channel != 'stable' }}
            type=ref,event=branch,enable=${{ matrix.channel != 'rc' }}

      - name: Build and push (full)
        uses: docker/build-push-action@v7
        with:
          context: .
          target: full
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-full.outputs.tags }}
          labels: ${{ steps.meta-full.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.channel }}
          cache-to: type=gha,mode=max,scope=${{ matrix.channel }}
          build-args: |
            FRESHELL_VERSION=${{ matrix.freshell_version }}
            CACHE_BUST=${{ github.run_id }}-${{ github.run_attempt }}

      # --- Lite variant (shares cached runtime-base from full build) ---

      - name: Extract metadata (lite)
        id: meta-lite
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=lite,enable=${{ matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=raw,value=lite-rc,enable=${{ matrix.channel != 'stable' && github.ref == 'refs/heads/main' }}
            type=raw,value=lite-${{ matrix.freshell_version }},enable=${{ matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=sha,suffix=-lite,enable=${{ matrix.channel != 'rc' }}
            type=sha,suffix=-lite-rc,enable=${{ matrix.channel != 'stable' }}
            type=ref,event=branch,suffix=-lite,enable=${{ matrix.channel != 'rc' }}

      - name: Build and push (lite)
        uses: docker/build-push-action@v7
        with:
          context: .
          target: lite
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta-lite.outputs.tags }}
          labels: ${{ steps.meta-lite.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.channel }}
          cache-to: type=gha,mode=max,scope=${{ matrix.channel }}
          build-args: |
            FRESHELL_VERSION=${{ matrix.freshell_version }}
