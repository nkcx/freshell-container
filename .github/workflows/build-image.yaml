name: Build and Publish Container

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  # Rebuild daily to pick up new freshell releases and security patches
  schedule:
    - cron: '0 6 * * *'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  resolve-freshell:
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.matrix.outputs.value }}
    steps:
      - name: Determine Freshell versions
        id: matrix
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Latest stable (non-prerelease) release
          STABLE=$(curl -sL -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/danshapiro/freshell/releases/latest \
            | jq -r '.tag_name')

          if [ -z "$STABLE" ] || [ "$STABLE" = "null" ]; then
            echo "ERROR: Could not determine stable Freshell version"
            exit 1
          fi

          # Latest prerelease (release candidate)
          RC=$(curl -sL -H "Authorization: Bearer $GH_TOKEN" \
            https://api.github.com/repos/danshapiro/freshell/releases \
            | jq -r '[.[] | select(.prerelease)] | sort_by(.created_at) | last | .tag_name // empty')

          # Compare versions: if RC is newer than stable, build it separately.
          # Otherwise (no RC, or RC is older/equal), rc tag points at stable.
          RC_IS_NEWER=false
          if [ -n "$RC" ] && [ "$RC" != "$STABLE" ]; then
            STABLE_SORT=$(echo "$STABLE" | sed 's/^v//')
            RC_SORT=$(echo "$RC" | sed 's/^v//')
            NEWER=$(printf '%s\n%s\n' "$STABLE_SORT" "$RC_SORT" \
              | sort -t. -k1,1n -k2,2n -k3,3n | tail -n1)
            if [ "$NEWER" = "$RC_SORT" ]; then
              RC_IS_NEWER=true
            fi
          fi

          # Build matrix: each channel × both variants (full, lite)
          if [ "$RC_IS_NEWER" = "true" ]; then
            MATRIX=$(jq -nc \
              --arg sv "$STABLE" --arg rv "$RC" \
              '{include:[
                {channel:"stable", freshell_version:$sv, variant:"full"},
                {channel:"stable", freshell_version:$sv, variant:"lite"},
                {channel:"rc",     freshell_version:$rv, variant:"full"},
                {channel:"rc",     freshell_version:$rv, variant:"lite"}
              ]}')
          else
            MATRIX=$(jq -nc \
              --arg sv "$STABLE" \
              '{include:[
                {channel:"stable-and-rc", freshell_version:$sv, variant:"full"},
                {channel:"stable-and-rc", freshell_version:$sv, variant:"lite"}
              ]}')
          fi

          echo "Stable: $STABLE, RC: ${RC:-none}, RC is newer: $RC_IS_NEWER"
          echo "value=$MATRIX" >> "$GITHUB_OUTPUT"

  build-and-push:
    needs: resolve-freshell
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix: ${{ fromJson(needs.resolve-freshell.outputs.matrix) }}
      fail-fast: false

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            # Floating tags (main branch pushes only)
            type=raw,value=latest,enable=${{ matrix.variant == 'full' && matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=raw,value=rc,enable=${{ matrix.variant == 'full' && matrix.channel != 'stable' && github.ref == 'refs/heads/main' }}
            type=raw,value=lite,enable=${{ matrix.variant == 'lite' && matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=raw,value=lite-rc,enable=${{ matrix.variant == 'lite' && matrix.channel != 'stable' && github.ref == 'refs/heads/main' }}
            # Freshell version tags (main branch pushes only)
            type=raw,value=${{ matrix.freshell_version }},enable=${{ matrix.variant == 'full' && matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            type=raw,value=lite-${{ matrix.freshell_version }},enable=${{ matrix.variant == 'lite' && matrix.channel != 'rc' && github.ref == 'refs/heads/main' }}
            # SHA tags (all builds including PRs — one per variant×channel)
            type=sha,enable=${{ matrix.variant == 'full' && matrix.channel != 'rc' }}
            type=sha,suffix=-rc,enable=${{ matrix.variant == 'full' && matrix.channel != 'stable' }}
            type=sha,suffix=-lite,enable=${{ matrix.variant == 'lite' && matrix.channel != 'rc' }}
            type=sha,suffix=-lite-rc,enable=${{ matrix.variant == 'lite' && matrix.channel != 'stable' }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: .
          target: ${{ matrix.variant }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.variant }}-${{ matrix.channel }}
          cache-to: type=gha,mode=max,scope=${{ matrix.variant }}-${{ matrix.channel }}
          build-args: |
            FRESHELL_VERSION=${{ matrix.freshell_version }}
            CACHE_BUST=${{ github.run_id }}-${{ github.run_attempt }}
